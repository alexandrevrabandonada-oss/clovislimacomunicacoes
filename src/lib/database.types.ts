// Placeholder for generated types from Supabase. Run `npm run db:types` after connecting.

export type Json = any

export type Database = {
  public: {
    Tables: {
      works: { Row: any }
      services: { Row: any }
      testimonials: { Row: any }
    }
  }
}
